import './SearchButton.scss';

function SearchButton() {
  return (
    <button
      className="search-button"
      type="submit"
      aria-label="Поиск"
      title="Поиск"
    />
  );
}

export default SearchButton;
